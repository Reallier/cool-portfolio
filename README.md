# cool-portfolio

Next.js App Router + Tailwind + framer-motion + three.js + r3f starter.

## 使用

```bash
npm i
npm run dev
```

可改 `app/page.tsx` 的名字和邮箱。3D 默认是 Procedural Blob。若放置 glTF 到 `public/models/hero.glb` 并将 `<ThreeHero useModel />` 即可启用模型。

## 部署
推送到 GitHub，用 Vercel 导入一键部署。
